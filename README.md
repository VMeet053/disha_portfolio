# Disha Hingu — Portfolio

Static site (HTML, CSS, vanilla JS). No build step — Vercel serves it as-is.

- `index.html` — all content
- `css/styles.css` — styles (warm editorial theme, fully responsive)
- `js/site.js` — menu, scroll reveals, word-by-word statement, services accordion, counters, contact form
- `assets/` — photo, favicon, social share image

**To-dos for Disha**
- Add a CV: save it as `assets/Disha-Hingu-CV.pdf` and uncomment the Download CV button in `index.html`.
- Swap live screenshot previews for static images in `assets/work/` for faster, reliable loading.
- Add testimonials back only with real client names and companies.
